aiohttp>=3.8.0
aiofiles>=23.0.0
websockets>=11.0.0
pydantic>=2.0.0
PyYAML>=6.0.0
tenacity>=8.0.0

# Optional dependencies
graphql-core>=3.2.0  # For GraphQL AST validation (optional)
orjson>=3.8.0        # For faster JSON processing (optional)